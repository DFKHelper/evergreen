import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Empty turbopack config to use defaults (which work well with file watching)
  turbopack: {},
};

export default nextConfig;
